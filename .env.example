# Ayra TRQP Profile API Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# API Configuration
API_TITLE="Ayra TRQP Profile API"
API_VERSION="1.0.0"

# External URL for Swagger (useful for ngrok tunneling)
# Example: EXTERNAL_URL=https://accurately-perfect-duck.ngrok-free.app
# If not set, defaults to http://localhost:8000
EXTERNAL_URL=

# CORS Configuration
CORS_ORIGINS=["*"]

# Database Configuration (for future implementation)
# DATABASE_URL=postgresql://user:password@localhost/trqp_db

# Trust Registry Configuration
# ECOSYSTEM_DID=did:example:ecosystem123
# TRUSTREGISTRY_DID=did:example:trustregistry456
# EGF_DID=did:example:egf789

# Logging Configuration
LOG_LEVEL=INFO

# NGROK Configuration for local development
# To use ngrok for exposing local server, set the following:
# NGROK_AUTH_TOKEN=your-ngrok-auth-token

# ==========================================
# Google OAuth Authentication Configuration
# ==========================================
# Required for admin interface access control
# Follow these steps to configure:
#
# 1. Go to Google Cloud Console: https://console.cloud.google.com
# 2. Create a new project or select an existing one
# 3. Enable Google+ API
# 4. Go to "Credentials" and create OAuth 2.0 Client ID
# 5. Application type: Web application
# 6. Add authorized redirect URIs:
#    - http://localhost:8000/admin/auth/callback (for local development)
#    - https://your-domain.com/admin/auth/callback (for production)
# 7. Copy the Client ID and Client Secret below

# Google OAuth Client ID (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (from Google Cloud Console)
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Authorized email addresses (comma-separated list of Google accounts with admin access)
# IMPORTANT: Set this to the Google account email addresses you want to authorize
# Examples:
#   Single user:   AUTHORIZED_EMAILS=admin@example.com
#   Multiple users: AUTHORIZED_EMAILS=admin@example.com,manager@example.com,dev@example.com
AUTHORIZED_EMAILS=your-email@example.com,another-admin@example.com

# Legacy support: Single authorized email (deprecated - use AUTHORIZED_EMAILS instead)
# If AUTHORIZED_EMAILS is not set, this will be used for backward compatibility
# AUTHORIZED_EMAIL=your-email@example.com

# Secret key for session encryption
# IMPORTANT: Generate a secure random string for production
# You can generate one using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# or by using: openssl rand -base64 32
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# ==========================================
# Optional Bearer-Token Admin Auth
# ==========================================
# Use one of the following approaches:
#
# 1) Static tokens (dev/test only):
#    ADMIN_BEARER_TOKENS=token1,token2
#
# 2) JWT validation via JWKS (recommended):
#    ADMIN_BEARER_JWKS_URL=https://idp.example.com/.well-known/jwks.json
#    ADMIN_BEARER_ISSUER=https://idp.example.com/
#    ADMIN_BEARER_AUDIENCE=trqp-admin
#
# When a Bearer token is provided, it is validated first. If no Bearer
# token is provided, the admin UI cookie-based OAuth flow is used.
ADMIN_BEARER_TOKENS=
ADMIN_BEARER_JWKS_URL=
ADMIN_BEARER_ISSUER=
ADMIN_BEARER_AUDIENCE=
